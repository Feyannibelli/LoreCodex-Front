import React from "react";

const ListsPage: React.FC = () => {
    return (
        <div className="p-6">
            <h1 className="text-3xl font-bold mb-4">Lists</h1>
            <p>Acá podemos crear y ver las listas creadas.</p>
        </div>
    );
};

export default ListsPage;
